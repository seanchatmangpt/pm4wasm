# Drift Status

Real admitted-vs-exercised coverage from the merged OCEL corpus.
`gates/010_coverage_floor.rq` refuses generation when coverage falls
below the configured floor.


No `cnv-ocel:DriftReport` observation is admitted in this graph; no coverage ratio is asserted.

